server:
  port: 8092
spring:
  application:
    name: service-producer
eureka:
  client:
    service-url:
      defaultZone: http://test:abc123@localhost:8761/eureka/
feign:
  hystrix:
    enabled: true
management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: refresh,health,info
    
writer: aaaaaaaaaaaa